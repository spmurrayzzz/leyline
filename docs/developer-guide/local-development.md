# Local development

Install dependencies:

```bash
npm install
```

Common commands:

```bash
npm run dev
npm run build
npm run preview
npm run electron:dev
npm run electron:build
npm run screenshot
npm run video
npm run video:mp4
```

For visual work, keep the Vite dev server running at `http://localhost:5173/` and validate with screenshots or the browser before moving on.
