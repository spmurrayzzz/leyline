# Realtime events

`GET /api/pi/events` opens the SSE stream consumed by the frontend EventSource adapter.

Events drive runtime running and idle state, live assistant text reveal, live tool rows, thinking traces, compaction events, debounced transcript refreshes, and runtime error surfacing.
