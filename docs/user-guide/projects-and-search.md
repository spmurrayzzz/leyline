# Projects and search

The sidebar groups sessions by project. Project groups can be expanded or collapsed, and the folder browser lets you choose a cwd for new sessions.

Sidebar search uses fuzzy matching against visible labels only, not hidden transcript content. Matched characters are highlighted in results.
