# Export

Transcript export produces self-contained HTML with embedded CSS and JavaScript.

Exports include rendered markdown, collapsed tool rows, thinking output, image data URLs, skill summaries, and lazy Pierre previews. When `LEYLINE_PUBLIC_URL` is set, export can include OG and Twitter metadata for public sharing.
