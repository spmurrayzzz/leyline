# Start screen

The start screen appears before a pi session is selected. It provides a large centered composer, project picker, folder browser, and runtime-state preview for the selected cwd.

Model and thinking choices can be staged before the startup run. When you create a new pi session from the start screen, those choices are applied as the session starts.
