# Settings

The settings drawer shows read-only runtime and session state.

Reload uses `POST /api/pi/reload` or the scoped session reload route. Reload refreshes keybindings, extensions, skills, prompts, and themes for the runtime.
