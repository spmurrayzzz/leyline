# Workbench

The workbench contains the topbar context, transcript, live runtime output, composer, optional drawers, and terminal.

The topbar stays stable: project name, cwd, message count, and modified time. During session switching, Leyline renders transcript detail as soon as it loads while runtime activation continues. Motion is restrained, and reduced-motion preferences are respected at a high level.
