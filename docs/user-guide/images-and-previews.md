# Images and previews

Leyline supports image input and image read rendering in transcripts.

Pierre previews display file, image, patch, and diff metadata. Inline previews are clipped to a line limit, and the fullscreen overlay renders full preview content when opened.
