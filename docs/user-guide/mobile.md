# Mobile layout

Below 760px, Leyline switches to a single-column workbench with a slide-over sidebar.

The transcript and composer are adjusted for small screens so reading and prompt entry remain usable without desktop-width panels.
